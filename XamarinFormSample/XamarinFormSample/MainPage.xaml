<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XamarinFormSample"
             x:Class="XamarinFormSample.MainPage"
             x:DataType="local:MainViewModel">
    <Grid>
        <ActivityIndicator IsVisible="{Binding IsLoading, Mode=OneWay}"/>
        <StackLayout IsVisible="{Binding IsNotLoading, Mode=OneWay}">
            <Entry Placeholder="Redirect Uri" Text="{Binding RedirectUri, Mode=TwoWay}" Margin="16, 16, 16, 0"/>
            <Entry Placeholder="Client Id" Text="{Binding ClientId, Mode=TwoWay}" Margin="16, 16, 16, 0"/>
            <Entry Placeholder="Authgear endpoint" Text="{Binding AuthgearEndpoint, Mode=TwoWay}" Margin="16, 16, 16, 0"/>
            <Label Text="SessionState" Margin="16"/>
            <Label Text="{Binding State, Mode=OneWay}" Margin="16, 0, 16, 0"/>
            <Button Text="Configure" Clicked="Configure_Clicked" Margin="16"/>
            <Button Text="Authorize" Clicked="Authorize_Clicked" Margin="16, 0, 16, 0"/>
        </StackLayout>
    </Grid>

</ContentPage>
